# Activates profile "test" for all @SpringBootTest / mvn test runs.
# These entries override src/main/resources/application.properties on the test classpath
# so ConfigData does not try to reach Vault before profile-specific files load.
spring.profiles.active=test
spring.cloud.vault.enabled=false
spring.config.import=
